# JavaSE

JavaSE learning project.

## Structure

- `src/` - Java source files
- `out/` - local build output, ignored by Git

## Run

Open the project in IntelliJ IDEA, or compile and run Java files from `src/` after installing a JDK.
